# AI 보조 도구 웹 설정 검토 결과

## 결론
제안한 구성은 전체적으로 방향이 맞고, 예시 궤적도 **Think → Search → Browse → Extract** 흐름을 잘 따르고 있습니다. 다만 실제 웹 배포/운영 시에는 아래 4가지를 보완해야 안정적으로 동작합니다.

## 보완 권장 사항

1. **System Instruction에 CoT 원문 직접 노출 금지 문구 추가**
   - 현재 문구는 CoT 데이터셋 구축 전문가 역할을 잘 정의하고 있으나, 최종 사용자에게 내부 추론(Think 단계의 상세 사고열)을 그대로 출력하지 않도록 명시해야 합니다.
   - 권장 문구 예시:
     - "내부 추론은 비공개로 유지하고, 최종 출력에는 검증 가능한 근거 요약만 제시한다."

2. **출력 스키마(JSON) 강제**
   - 웹 환경 자동화에서는 자유 텍스트보다 고정 스키마가 중요합니다.
   - 최소 필드 권장:
     - `domain`
     - `seed_answer`
     - `obfuscation`
     - `final_question`
     - `trajectory` (step 배열: think/search/browse/extract)
     - `final_answer`
     - `evidence` (URL, 인용문)

3. **검증 게이트(Validation Gate) 추가**
   - `extract` 직전에 다음 자동 체크를 넣어야 품질이 올라갑니다.
     - 질문과 정답의 1:1 대응 여부
     - 모호화 문장에 정답 직노출 여부
     - 날짜/명칭/고유명사의 출처 일치 여부

4. **한국어 문체 규칙 예외 처리 정의**
   - "-습니다 체" 규칙은 좋지만, 고유명사/인용문/영문 원문은 예외로 처리해야 자연스럽습니다.
   - 예: 게임명, DLC명, 공식 발표문 원문 인용은 원문 보존 허용.

## 제시한 시뮬레이션 예시 검토

- **강점**
  - 도메인 입력("비디오 게임")에서 구체 개체(엘든 링 DLC)로 좁혀지는 과정이 자연스럽습니다.
  - 검색→공식 페이지 재확인 흐름이 있어 신뢰성 확보에 유리합니다.

- **수정 권장 포인트**
  - Step 2/4의 search는 스니펫 의존이므로, 최소 1회는 공식/1차 출처를 강제하는 규칙이 필요합니다.
  - Step 6의 browse는 URL을 실제 값으로 남겨 재현 가능성을 높여야 합니다.
  - `Signal: positive` 기준(무엇을 만족하면 positive인지)을 정량 규칙으로 명시해야 합니다.

## 웹 설정에 바로 넣을 수 있는 운영 규칙(요약)

- System: 역할 + 비공개 추론 + 한국어 문체 + 단계 강제
- Developer: JSON 스키마 강제 + 검증 게이트 + 실패 시 재시도 정책
- Runtime:
  - search 최대 N회
  - browse 최소 1회(가능하면 2개 출처 교차검증)
  - evidence 2개 이상 수집 시만 `final_answer` 확정

## 최종 판단
현재 설계는 **파일럿 수준으로 사용 가능**합니다. 다만 운영 단계(웹 서비스 배포)로 가려면 **출력 스키마 고정, 근거 검증 규칙, CoT 비노출 안전장치**를 반드시 추가하는 것이 좋습니다.
